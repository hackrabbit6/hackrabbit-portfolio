# PERSONAL-SITE

站点由「个人作品站」改为「个人主页」。本文档描述这次改造。

先读 `AGENTS.md`（边界）与 `CONTEXT.md`（术语）。上一轮视觉整改见 `VISUAL-POLISH.md`
—— 那一轮的字号刻度、间距节奏、Cover 渲染管线、移动端与可访问性结论继续有效，不要推翻重做。

**严格按 phase 顺序做。** 每个 phase 结束跑 `bun run lint` 与 `bun run build`，全绿才进入下一个。

---

## 为什么改

原来的站只回答一个问题：「这个人交付过什么」。读者只有招聘方。

现在要同时服务两类读者，而且他们**技术水平相差很远**：

| 读者 | 从哪来 | 想知道什么 |
|---|---|---|
| 内容读者 | 公众号 / 小红书 | 这人是谁、在做什么、去哪关注 |
| 招聘方与技术面试官 | 简历里的网址 | 能不能合作或雇佣 |

**判断标准：一个从自媒体点进来的非技术读者，能不能在首屏附近找到关注入口、并且看得懂首页在说什么。**

---

## 八条已定的决定

| # | 决定 | 后果 |
|---|---|---|
| 1 | **独立优先** —— 独立在前，受雇在后，顺序体现在版面权重 | 消解「独立开发」与「我在找工作」的自相矛盾 |
| 2 | 首页主角 = **在做什么**（在造的东西） | Selected Work 让位 |
| 3 | `NOW · CREW` 薄带**拆掉** | NOW 并入主角，CREW 独立成首页小区块 |
| 4 | 关注入口**紧跟主角之后** | 读者刚看完你在造什么，此刻关注意愿最强 |
| 5 | 边界：主角=**造**，`/now/`=**看** | 同一件事不写两处 |
| 6 | 首屏语序：**现在 X。此前 4-5 年前端交付。** | 语序本身编码优先级 |
| 7 | Contact：**合作在前，全职在后** | 页尾与首屏口径一致 |
| 8 | **首页降技术味，`/work/` 保留开发者审美** | 见 `docs/adr/0002-two-visual-registers.md` |

---

## 首页骨架

```
HERO           现在 X。此前 4-5 年前端交付。   [ 在做什么 ↓ ] [ GitHub ↗ ]
在做什么   ★    主角 —— 在造的东西
关注入口        自媒体账号
CREW           Claude Code / Codex / herdr
SELECTED WORK  精选 2–3 个 → /work/
EXPERIENCE     履历（数据不动）
CONTACT        接项目 / 也看全职
```

`/now/` 与 `/writing/` 是二级页，首页只留入口。

---

## 路由集合

```
/                    个人主页
/now/                在看的东西：书 / 人 / 研究方向
/writing/            列表：站内长文 + 外链短内容混排
/writing/[slug]/     站内正文（Astro content collection）
/work/               完整作品集（保留现状，保留开发者审美）
/work/h5-game-sdk/   保留现状
/resume.pdf          保留
```

内容承载方式为**混合**：长文 / 技术笔记写在站内 markdown，短内容与视频外链到平台。

---

## 硬约束（未解除）

- **不得编造事实。** 账号、平台、书名、研究方向、工具用途，全部只能来自本人提供。找不到就不写这一块。
- **不得留占位文案。** 没有内容就不上对应路由。空页面比没有页面更差。
- **不得引入 CMS / 后端 / 评论 / 搜索。** 文章是仓库里的 `.md`，不是数据库。
- **本仓库公开。** 不写个人隐私、求职策略、自身弱点分析或内容运营数据。

---

## P0 — 事实订正

### P0.1 CREW 三行 —— 已完成

移除已停用的 OpenSpec，替换为 herdr（`多 agent 管理 / 运行状态`，本人原话改写，
未采用工具官方功能描述）。

### P0.2 关注入口 —— 阻塞

首页新增关注入口区块，位置紧跟「在做什么」之后。每个账号给出平台、账号名、链接，
以及一句话说明这个号在讲什么。

**阻塞于：小红书与公众号的账号名和链接。**

---

## P1 — 首页改为个人主页

按上面的骨架重排，并执行决定 6、7、8。

- Selected Work 降为精选 2–3 个，完整列表在 `/work/`。
  这条与 `CONTEXT.md` 中 Selected Work 的旧定义冲突 —— 该定义**已同步改掉**。
- `src/components/Experience.tsx` 的数据依然不许动（`AGENTS.md`）。
- 沿用 `VISUAL-POLISH.md` 的字号刻度与间距节奏，只改语气（中文段落标签、少用等宽字体），
  不另起一套刻度。

---

## P2 — `/writing/`

列表页：站内文章与外链条目混排，倒序，外链标出平台名。
站内正文用 Astro content collection，文章是 `src/content/` 下的 `.md`。

已有五篇真实发布内容可用（标题与日期已确认），**阻塞于每篇的公开链接**。

---

## P3 — `/now/`

只放「在看的东西」：在读的书、在追的人、在想的研究方向。标注最后更新日期。

维护风险：这一页明确宣称「这是我此刻的状态」，长期不更新的伤害大于没有这一页。
做之前先确认能维持更新节奏。

---

## 内容缺口

| # | 内容 | 用在哪 | 状态 |
|---|---|---|---|
| 1 | 小红书 / 公众号：账号名 + 链接 + 一句话说明 | P0.2 关注入口 | ❌ 阻塞 |
| 2 | 五篇已发布文章的公开链接 | P2 `/writing/` | ❌ 阻塞 |
| 3 | 在读的书 / 在追的人 | P3 `/now/` | ❌ |
| 4 | herdr 用途 | CREW | ✅ 已上线 |
| 5 | X = 现在在做什么 | Hero、主角区块 | ✅ 已确认 |
